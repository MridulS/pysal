language: python
sudo: false
branches:
only:
  - master
python:
  - 2.7
  #- "2.6"
  - 3.4

env:
  - SCIPY_VERSION=0.17.0 NUMPY_VERSION=1.10.1
  - SCIPY_VERSION=0.16.1 NUMPY_VERSION=1.10.0
  - SCIPY_VERSION=0.16.0 NUMPY_VERSION=1.9.2

before_install:
  - wget https://repo.continuum.io/miniconda/Miniconda-latest-Linux-x86_64.sh -O miniconda.sh
  - chmod +x miniconda.sh
  - ./miniconda.sh -b -p ./miniconda
  - export PATH=`pwd`/miniconda/bin:$PATH
  - conda update --yes conda
  - conda create -y -q -n test-env python=$TRAVIS_PYTHON_VERSION
  - source activate test-env
  - chmod +x ./.travis_testing.sh
  - if [[ $TRAVIS_PYTHON_VERSION == 3* ]]; then rm -rf pysal/contrib; 2to3 -nw pysal/ > /dev/null; fi

install:
  - conda install --yes nose pip
  - pip install -r travis.txt
  - pip install numpy==$NUMPY_VERSION scipy==$SCIPY_VERSION

script:
  - python -c 'import numpy; print(numpy.__version__)'
  - python -c 'import scipy; print(scipy.__version__)'
  - python setup.py install  >/dev/null
  - python -c 'import pysal'
  #- python -c 'import multiprocessing as mp; print mp.cpu_count()'
  #- pychecker --limit 1000 pysal
  - python setup.py sdist >/dev/null
  #- nosetests --processes=-1 --process-timeout=60 --verbosity=1 --ignore-files=collection --exclude-dir=pysal/contrib
  #- nosetests
  - echo "check_stable=False" >pysal/config.py
  - ./.travis_testing.sh
  #- cd doc; make pickle; make doctest
notifications:
    email:
        recipients:
            - sjsrey@gmail.com
            - levi.john.wolf@gmail.com
        on_success: always
        on_failure: always

after_success:
  - coveralls
